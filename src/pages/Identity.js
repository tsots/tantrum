import React, { Component } from 'react';
import Content from '../sections/Identity/Content';

class Identity extends Component {
  render() {
    return (
      <div>
        <Content />
      </div>
    );
  }
}

export default Identity;
